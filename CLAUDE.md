# ScaleLab — Claude Code Project Context

This file is auto-loaded by Claude Code at the start of every session.
It gives Claude the full project context so you never have to re-explain it.

---

## What this project is

A self-directed lab for practising high-performance backend patterns in ASP.NET Core 9.
The goal is to build on the same product-catalogue domain, adding one scalability technique per iteration.

**Current iteration:** Cache-Aside Pattern with Redis
**Next planned iteration:** Clean Architecture refactor (see `agent-instructions.md`)

---

## Tech stack

| Layer | Technology |
|-------|-----------|
| API | ASP.NET Core 9 — controller-based |
| ORM | Entity Framework Core 9 |
| Database | SQL Server 2022 |
| Cache | Redis via `IDistributedCache` + StackExchange.Redis |
| Fake data | Bogus |
| Containers | Docker Compose |

---

## Current project layout

```
ScaleLab.Api/
└── ScaleLab.Api/               ← single project (pre-Clean-Architecture)
    ├── Controllers/
    │   └── ProductsController.cs
    ├── Data/
    │   ├── AppDbContext.cs
    │   └── DataSeeder.cs
    ├── Models/
    │   └── Product.cs
    ├── Migrations/             ← generated by EF Core
    ├── Program.cs
    └── appsettings.json
```

---

## Established patterns and conventions

| Convention | Detail |
|-----------|--------|
| Namespaces | File-scoped (`namespace Foo.Bar;`) |
| Async | `async/await` throughout — no `.Result` or `.Wait()` |
| Read queries | Always `.AsNoTracking()` |
| Seeding | Batches of 10,000 with `ChangeTracker.Clear()` after each `SaveChanges()` |
| Caching | Cache-Aside: check Redis → hit returns `source:"Cache"`, miss writes to Redis then returns `source:"Database"` |
| Pagination | `Skip((page-1)*pageSize).Take(pageSize)`, pageSize capped at 100 |
| Cache keys | `products_page_{page}_size_{pageSize}`, prefixed `ScaleLab:` via `InstanceName` |
| Cache TTL | 5-minute absolute expiry |
| Serialization | `System.Text.Json` with `JsonNamingPolicy.CamelCase` |

---

## Infrastructure

### Connection strings

| Environment | SQL Server | Redis |
|-------------|-----------|-------|
| `dotnet run` (local) | `Server=localhost,1433;...` | `localhost:6379` |
| Docker Compose | `Server=db,1433;...` | `redis:6379` |

Docker Compose overrides `appsettings.json` via `ConnectionStrings__DefaultConnection` and `ConnectionStrings__Redis` environment variables.

### Docker Compose ports

| Port | Service |
|------|---------|
| 5000 | ASP.NET Core API |
| 1433 | SQL Server (TDS — not HTTP) |
| 6379 | Redis |

Both SQL Server and Redis have health checks. The API uses `condition: service_healthy` for both so it never starts before they're ready.

### Startup sequence (Program.cs)

1. Register services (EF Core, Redis cache, controllers, Swagger)
2. Build and configure middleware pipeline
3. In a scoped block: run `db.Database.Migrate()`, then seed if `Products` table is empty

---

## EF Core migrations

Always run from `ScaleLab.Api/ScaleLab.Api/` (the directory containing the `.csproj`):

```bash
dotnet restore
dotnet ef migrations add <MigrationName>
```

Migrations are applied automatically on startup via `db.Database.Migrate()`.

---

## Key reference files

| File | Purpose |
|------|---------|
| `NOTES.md` | Learning notes — patterns, concepts, gotchas |
| `TROUBLESHOOTING.md` | Errors encountered and their fixes |
| `agent-instructions.md` | Step-by-step instructions for the Clean Architecture refactor |
| `README.md` | Getting-started guide for running the project |

---

## Behaviour guidelines for Claude

- Ask before implementing if any requirement is ambiguous or seems wrong.
- Do not add error handling, abstractions, or features beyond what is explicitly requested.
- Do not write comments that describe what the code does — only write a comment when the *why* is non-obvious.
- Update `NOTES.md` when a new pattern or concept is introduced.
- Update `TROUBLESHOOTING.md` when a new error and fix is encountered.
- Keep responses concise. No trailing summaries of what was just done unless asked.
